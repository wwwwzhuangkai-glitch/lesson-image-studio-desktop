# Lesson Image Studio 本轮重构计划

## Summary

本轮作为一次完整交付，先修正确性，再做前端工作台重构，最后补齐“适合 AI 接力开发”的最终文档。目标不是局部润色，而是把当前“超长网页”改成贴近你 5 张 JPG 参考图的单屏工作台：固定应用壳、稳定左栏、轻量顶栏、中间主画布、右侧检视器，次级信息不再整页摊开。

本轮已经锁定的产品决定：

- 版本绝不允许跨图片项引用
- `base_version` 只能属于当前 `image_item`
- `mask` 只能基于当前 `image_item` 自己的版本创建
- 真有复用需求时，走“复制为新图片项起点”，而不是跨图串版本
- 前端错误提示直接透传后端 `detail`
- 最近打开固定取 `5`
- 编辑页是单屏工作台
- 右侧检视器常驻 5 类核心能力：提示词、参数设置、定稿、导出、发布
- 模板默认隐藏，放在右侧检视器折叠区，不用弹窗
- 总览页采用“顶部薄摘要 + 紧凑卡片矩阵”
- 左栏底部作为任务/事件/回收站入口
- 本轮同时交付“复制为新图片项起点”功能
- `docs/` 以中文为主、保留英文代码名，且只写实现后的最终文档

## Key Changes

### 1. 正确性与接口修正

- 修复 `/api/masks` 与 `/api/edits` 的数据完整性校验：
  - 若 `base_version_id` 不属于传入的 `image_item_id`，接口直接返回 `400`
  - `mask_id` 若存在，也必须属于同一个 `image_item`
  - 后端 service 层保留兜底校验，不能只靠 API 层
- 新增“复制为新图片项起点”能力：
  - 新增接口：`POST /api/versions/{versionId}/duplicate-to-image-item`
  - 入参：`title?`
  - 行为：在同一 `Owner` 下创建新的 `ImageItem`，并把该版本复制成新图片项的根版本
  - 复制出的根版本必须 `parent_version_id = null`，不与原图版本树相连
  - 默认标题可由服务端生成“原标题 - 副本”，前端允许老师改名
- 修复前端请求层错误透传：
  - `response.ok = false` 时优先展示后端 `detail`
  - 不再把友好错误吞成整段 JSON
- 修复最近打开配置流：
  - `recent_owner_limit` 必须来自当前 app 实例的 settings
  - `GET /api/owners/recent` 与 `POST /api/owners/open` 返回的 recent 列表都遵循该配置
  - 默认值本轮统一为 `5`

### 2. 前端工作台重构

- 整体改成固定应用壳，而不是页面式长滚动：
  - 左侧固定栏
  - 顶部轻量工具栏
  - 中央主内容区
  - 右侧固定检视器
  - 各区域内部独立滚动，主页面不再靠整页下拉
- 左栏重构：
  - 总览页：显示当前 Owner、主导航语义、图片项入口
  - 编辑页：显示紧凑版本树列表，节点带小缩略预览与状态
  - 左栏底部提供任务中心、事件流、回收站入口
  - 这些入口统一打开右侧 utility drawer，并切换到对应 tab
- 顶部工具栏改为轻量模式：
  - 只保留返回、当前 Owner/图片标题、关键状态、少量全局入口
  - 不引入搜索和后台化筛选，避免破坏参考图的简洁感
- 总览页重构为“顶部薄摘要 + 紧凑图片项矩阵”：
  - 图片项卡片以缩略图识别为主，状态信息为辅
  - 卡片保留最少必要动作：进入编辑、导入底图、复制起点、删除/恢复
  - 指标区压缩成很薄的摘要条，不再像仪表盘大块堆叠
- 编辑页重构为标准三栏工作台：
  - 左侧：版本树
  - 中间：主画布 + 画布上方迷你工具条
  - 右侧：检视器
- 画布区重构：
  - 前后对比、框选改图等局部操作放到画布上方迷你工具条
  - 右侧检视器只负责参数和状态，不承接所有局部画布动作
- 右侧检视器重构：
  - 常驻：提示词、质量/尺寸、定稿、导出、发布
  - 模板区放在底部折叠区，默认收起
  - 不再把模板区、删除区、杂项区无限向下堆成长栏
- 次级信息重构：
  - 任务中心、事件流、回收站统一为右侧工具抽屉
  - 入口来自左栏底部
  - 抽屉内部用 tabs/segmented control 切换，不再维持多个互相独立的浮层
- 视觉方向统一对齐参考图：
  - 浅雾蓝灰背景
  - 固定大卡片式工作台
  - 柔和分区、轻玻璃感、紧凑留白
  - 明确压缩纵向节奏，避免“AI 功能面板越堆越长”

### 3. 交互与数据流补强

- 编辑页新增“复制为新图片项起点”入口：
  - 基于当前选中版本触发
  - 成功后跳转到新图片项编辑页或返回总览高亮新卡片，默认采用“直接进入新图片项”
- 版本树与图片项关系保持强约束：
  - 任何版本查询、遮罩创建、改图提交、复制起点，都以当前 `image_item` 为边界
- 当前错误提示策略统一：
  - 前端通知直接显示后端 `detail`
  - 不另做面向终端用户的柔化包装
- 模板区仍保留系统模板与个人模板：
  - 默认折叠
  - 展开后可查看全文
  - 系统模板仅可复制后编辑
- 现有空图片项流继续保留：
  - 空图片项可导入底图
  - 空图片项可直接 AI 生图
  - 复制起点得到的新图片项属于“已有根版本”的非空图片项

### 4. 文档与 AI 接力交付

- 本轮实现完成后，`docs/` 至少补齐这 4 类最终文档：
  - 架构与数据流文档：说明当前真实结构、关键实体边界、接口主链路
  - UI 工作台说明：说明总览页/编辑页/utility drawer/右检视器的最终骨架与区域职责
  - AI 接力说明：列出关键不变量、扩展点、常见改动入口、容易踩坑处
  - TODO / backlog：按下一轮能力切分，而不是零散想法罗列
- 文档语言采用中文，保留英文代码名、接口名、类型名
- 文档只记录“当前真实实现”，不保留已经过期的旧布局描述

## Test Plan

- 后端新增/更新测试：
  - `item B + version A` 调 `/api/masks` 返回 `400`
  - `item B + version A` 调 `/api/edits` 返回 `400`
  - `mask_id` 不属于当前图片项时返回 `400`
  - 复制起点会创建新 `ImageItem` 和新根版本，且 `parent_version_id = null`
  - 复制起点后的新旧版本树彼此独立
  - `recent_owner_limit=5` 时 recent 接口严格返回 5 条以内
  - app 级自定义 `recent_owner_limit` 生效，不再读全局默认
- 前端新增/更新测试：
  - 后端返回 `detail` 时，通知显示可读文案而不是 JSON
  - 编辑页渲染为单屏三栏骨架，模板区默认折叠
  - 左栏底部入口能打开统一 utility drawer 并切换到任务/事件/回收站
  - 总览页卡片矩阵以缩略图为主，关键状态可见
  - 从选中版本触发“复制为新图片项起点”后，成功进入新图片项
- 人工验收场景：
  - 在同一 Owner 下创建 A/B 两张图，B 图不能选 A 图版本去改图或建 mask
  - 选中 A 图某版本复制成新图片项，得到独立的新版本树
  - 编辑页首屏不再是长滚动，主画布与右检视器同时可见
  - 模板区默认收起，展开后可用，关闭后不影响主工作流
  - 左栏底部能打开任务、事件、回收站，且不破坏主工作台布局

## Assumptions

- 本轮是同一轮交付：先落 review 正确性修复，再落前端重构
- `duplicate-to-image-item` 默认在同一 `Owner` 下复制，不支持跨 Owner 复制
- 复制起点后的新版本与原版本不建立树关系，只在事件记录里保留来源痕迹
- 编辑页与总览页都采用桌面优先设计；移动端仅保证基本可访问，不追求同等工作台体验
- 统一 utility drawer 采用右侧抽屉呈现，入口放在左栏底部
- 顶部工具栏保持轻量，不在本轮引入全局搜索或复杂筛选
